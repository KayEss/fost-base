#pragma once


#include <felspar/memory.hpp>


namespace fostlib {


    /// A non-owning view of part of an array of `V`. May be mutable or
    /// immutable depending on the type `V`.
    template<typename V>
    using array_view = felspar::buffer<V>;


}

